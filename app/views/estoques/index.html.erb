<br/>
 <table  align="center" width="100%">
  <td>
  <fieldset><legend><font  size=5>ESTOQUE INFORMÁTICA SEDUC</font></legend>
   <br/>
   <font  size="2">
   <div class="botao">
   <table width="100%">
    <tr>
     <td align="left" width="6%"><%= button_to 'Entrada', new_entrada_path %></td>
     <td align="left" width="6%"><%= button_to 'Saida', new_saida_path %></td>
      <td align="left" width="6%"><%= button_to 'Voltar', homes_path, {:method => "get"}%></td>
     <td align="left" width="6%"><input type="Button" Value="Imprimir" onclick="javascript:window.print()"></td>
      <% if  $var == 0 then %>
        
      <%end%>
      <td align="right">
       <div class="botao">
         <table>
        <tr>
        <% form_tag estoques_path, :method => 'get' do %>
        <td align="right"><font size="4"> Digite Produto:</font>
           <%= text_field_tag :search, params[:search] %>
           <%= submit_tag "Filtrar", :nome => nil %>
        </td>
        <% end %>
        </tr>
     </table>
     </div>
     </td>
     </tr>
  </table>
  <br/>
  </div>
   <table align="center"  border="2" width="100%">
      <tr>
        <th bgcolor="gray">Produto</th>
        <th bgcolor="gray">Especificação</th>
        <th bgcolor="gray">Saldo</th>
        <th bgcolor="gray">Observação</th>
      </tr>
      <%$totalc = 0%>
      <% for estoques in @estoques %>
     <tr>
        <td><%=h estoques.componente.nome %></td>
        <td><%=h estoques.especifica %></td>
        <td><%=h estoques.saldo %></td>
        <td><%=h estoques.obs %></td>
      </tr>
      <%$totalc= $totalc + estoques.saldo%>
      <tr>
        <td colspan="4">
          <div class="botao">
          <table>
             <td><%= button_to('Exibir',{:action => "show", :id =>estoques}, :method => "get") %></td>
             <% if current_user.has_role?('administrador') %>
               <td> <%= button_to('Editar',{:action => "edit", :id =>estoques}, :method => "get") %></td>
               <td><%= button_to('Excluir',{:action => "destroy", :id =>estoques}, :confirm => 'Confirma exclusão?', :method => "delete")%></td>
             <%end%>
          </table>
          </div>
        </td>
      </tr>
      <%end%>
      <tr>
       <td colspan="3" align="left"><font  color ="black "size="2"><b> Total <%if $var1 == 1 %><%=h estoques.componente.nome %><%end%>: <%= $totalc %></b></font> </td>
      </tr>

      
 </table>
 </font>
 </fieldset>
</td>
</table>
<div class="botao">
   <table width="100%" align="center">
    <tr>
      <% if current_user.has_role?('administrador') %> 
        <td width="8%"><%= button_to 'Cadastrar', new_estoque_path %></td>
      <%end%>
      <td align="left" width="8%"><%= button_to 'Entrada', new_entrada_path %></td>
      <td align="left" width="8%"><%= button_to 'Saida', new_saida_path %></td>
      <td width="8%"><%= button_to 'Voltar', homes_path, {:method => "get"}%></td>
      <td width="8"align="left"><input type="Button" Value="Imprimir" onclick="javascript:window.print()"></td>
      <% if  $var == 0 then %>
         
      <%end%>

    </tr>
  </table>
  </div>
  <br/>