    <div class="botao">
        <table width="100%">
          <tr>
            <% form_for @search do |f| %>
              <tr>
                <td align="right">
                  <%= f.label :estagiario_id_equals, "Região" %>
                  <%= f.collection_select :estagiario_id_equals, Estagiario.all, :id, :nome, :include_blank => true%>
                </td>
                <td align="center" colspan="2">
                  <%= f.submit "Buscar" %>
                </td>
              </tr>
            <% end %>
          </tr>
        </table>
    </div>
    <%= render :partial => "calendar" %>

