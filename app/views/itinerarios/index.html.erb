    <div class="botao">
        <table width="100%">
          <tr>
            <% form_for @search do |f| %>
              <tr>
                <td align="right" width="95%"><font size="3">
                  <%= f.label :estagiario_id_equals, "Estagiário:" %>
                  <%= f.collection_select :estagiario_id_equals, @estagiarios, :id, :nome, :include_blank => true%></font>
                </td>
                <td align="left">
                  <%= f.submit "Buscar" %>
                </td>
              </tr>
            <% end %>
          </tr>
        </table>
    </div>
    <%= render :partial => "calendar" %>

